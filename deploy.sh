#!/bin/bash
# Simple deploy script for workflow enforcer
# Actual deployment happens via git push to Render

echo "Deployment triggered via git push"
echo "Render will auto-deploy from main branch"
exit 0
