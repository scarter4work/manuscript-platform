# Skip optional dependencies during install
# This prevents platform-specific packages (like @rollup/rollup-win32-x64-msvc)
# from causing EBADPLATFORM errors on CI/CD servers
# Modern npm syntax (v7+)
omit=optional
